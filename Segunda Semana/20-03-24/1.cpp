#include <iostream>

using namespace std;

int main() {
    int length{0};
    int Area{0};
    int Volum{0};

    //Area = 6 * (length * length);
    //Volum = length * length * length;

    length = 0;
    //cout << "Lenght " << length << endl;
    Area = 6 * (length * length);
    Volum = length * length * length;
    //cout << "Area is " << Area << " And Volume is " << Volum << endl;
    cout << length << "\t\t\t\t" << Area << "\t\t\t\t" << Volum << "\t\t\t\t" << endl;

    length = 1;
    //cout << "Lenght " << length << endl;
    Area = 6 * (length * length);
    Volum = length * length * length;
    //cout << "Area is " << Area << " And Volume is " << Volum << endl;
    cout << length << "\t\t\t\t" << Area << "\t\t\t\t" << Volum << "\t\t\t\t" << endl;

    length = 2;
    //cout << "Lenght " << length << endl;
    Area = 6 * (length * length);
    Volum = length * length * length;
    //cout << "Area is " << Area << " And Volume is " << Volum << endl;
    cout << length << "\t\t\t\t" << Area << "\t\t\t\t" << Volum << "\t\t\t\t" << endl;

    length = 3;
    //cout << "Lenght " << length << endl;
    Area = 6 * (length * length);
    Volum = length * length * length;
    //cout << "Area is " << Area << " And Volume is " << Volum << endl;
    cout << length << "\t\t\t\t" << Area << "\t\t\t\t" << Volum << "\t\t\t\t" << endl;

    length = 4;
    //cout << "Lenght " << length << endl;
    Area = 6 * (length * length);
    Volum = length * length * length;
    //cout << "Area is " << Area << " And Volume is " << Volum << endl;
    cout << length << "\t\t\t\t" << Area << "\t\t\t\t" << Volum << "\t\t\t\t" << endl;

    return 0;
}